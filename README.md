# Multi-neural-network
This project implements a fully connected neural network from scratch using NumPy without relying on deep learning frameworks. The goal is to understand the fundamental concepts behind neural networks, including forward propagation, backpropagation, gradient descent, and performance evaluation.
